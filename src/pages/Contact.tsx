import ContactSection from '@/components/ContactSection'

/**
 * Contact (/contact) — the contact form on its own dedicated page, reached via
 * the "Contact us" button in the header.
 */
export default function Contact() {
  return <ContactSection />
}
